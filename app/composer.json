{
    "name": "root/app",
    "description": "composer dans un docker conteneur",
    "require": {
        "altorouter/altorouter": "^2.0",
        "symfony/var-dumper": "^7.3",
        "symfony/dotenv": "^7.3"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "authors": [
        {
            "name": "Syleo"
        }
    ],
    "scripts": {
        "hello": "echo 'Hello depuis Composer'",
        "start": "php -S localhost:8000 -t public"
    }
}